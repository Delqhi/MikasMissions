{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "child.session.capped.v1",
  "type": "object",
  "required": ["child_profile_id", "session_id", "capped_at", "reason", "limit_minutes"],
  "properties": {
    "child_profile_id": {"type": "string"},
    "session_id": {"type": "string"},
    "capped_at": {"type": "string"},
    "reason": {"type": "string"},
    "limit_minutes": {"type": "integer", "minimum": 1}
  },
  "additionalProperties": true
}
