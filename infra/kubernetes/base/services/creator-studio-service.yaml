apiVersion: apps/v1
kind: Deployment
metadata:
  name: creator-studio-service
  namespace: mikasmissions-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: creator-studio-service
  template:
    metadata:
      labels:
        app: creator-studio-service
    spec:
      containers:
        - name: creator-studio-service
          image: ghcr.io/delqhi/mikasmissions/creator-studio-service:latest
          envFrom:
            - configMapRef:
                name: platform-runtime
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: platform-secrets
                  key: database-url
                  optional: true
          ports:
            - containerPort: 8087
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8087
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8087
            initialDelaySeconds: 15
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: creator-studio-service
  namespace: mikasmissions-dev
spec:
  selector:
    app: creator-studio-service
  ports:
    - port: 80
      targetPort: 8087
