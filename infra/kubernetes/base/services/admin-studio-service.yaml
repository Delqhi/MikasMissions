apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-studio-service
  namespace: mikasmissions-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: admin-studio-service
  template:
    metadata:
      labels:
        app: admin-studio-service
    spec:
      containers:
        - name: admin-studio-service
          image: ghcr.io/delqhi/mikasmissions/admin-studio-service:latest
          envFrom:
            - configMapRef:
                name: platform-runtime
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: platform-secrets
                  key: database-url
                  optional: true
          ports:
            - containerPort: 8090
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8090
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8090
            initialDelaySeconds: 15
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: admin-studio-service
  namespace: mikasmissions-dev
spec:
  selector:
    app: admin-studio-service
  ports:
    - port: 80
      targetPort: 8090
