apiVersion: apps/v1
kind: Deployment
metadata:
  name: moderation-service
  namespace: mikasmissions-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: moderation-service
  template:
    metadata:
      labels:
        app: moderation-service
    spec:
      containers:
        - name: moderation-service
          image: ghcr.io/delqhi/mikasmissions/moderation-service:latest
          envFrom:
            - configMapRef:
                name: platform-runtime
          ports:
            - containerPort: 8088
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8088
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8088
            initialDelaySeconds: 15
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: moderation-service
  namespace: mikasmissions-dev
spec:
  selector:
    app: moderation-service
  ports:
    - port: 80
      targetPort: 8088
