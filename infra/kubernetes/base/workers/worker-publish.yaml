apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker-publish
  namespace: mikasmissions-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: worker-publish
  template:
    metadata:
      labels:
        app: worker-publish
    spec:
      containers:
        - name: worker-publish
          image: ghcr.io/delqhi/mikasmissions/worker-publish:latest
          envFrom:
            - configMapRef:
                name: platform-runtime
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: platform-secrets
                  key: database-url
                  optional: true
