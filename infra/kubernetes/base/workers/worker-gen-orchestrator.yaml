apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker-gen-orchestrator
  namespace: mikasmissions-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: worker-gen-orchestrator
  template:
    metadata:
      labels:
        app: worker-gen-orchestrator
    spec:
      containers:
        - name: worker-gen-orchestrator
          image: ghcr.io/delqhi/mikasmissions/worker-gen-orchestrator:latest
          envFrom:
            - configMapRef:
                name: platform-runtime
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: platform-secrets
                  key: database-url
                  optional: true
