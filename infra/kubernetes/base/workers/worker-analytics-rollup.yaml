apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker-analytics-rollup
  namespace: mikasmissions-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: worker-analytics-rollup
  template:
    metadata:
      labels:
        app: worker-analytics-rollup
    spec:
      containers:
        - name: worker-analytics-rollup
          image: ghcr.io/delqhi/mikasmissions/worker-analytics-rollup:latest
          envFrom:
            - configMapRef:
                name: platform-runtime
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: platform-secrets
                  key: database-url
                  optional: true
