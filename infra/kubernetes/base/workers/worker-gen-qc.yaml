apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker-gen-qc
  namespace: mikasmissions-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: worker-gen-qc
  template:
    metadata:
      labels:
        app: worker-gen-qc
    spec:
      containers:
        - name: worker-gen-qc
          image: ghcr.io/delqhi/mikasmissions/worker-gen-qc:latest
          envFrom:
            - configMapRef:
                name: platform-runtime
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: platform-secrets
                  key: database-url
                  optional: true
