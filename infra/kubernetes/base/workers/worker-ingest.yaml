apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker-ingest
  namespace: mikasmissions-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: worker-ingest
  template:
    metadata:
      labels:
        app: worker-ingest
    spec:
      containers:
        - name: worker-ingest
          image: ghcr.io/delqhi/mikasmissions/worker-ingest:latest
          envFrom:
            - configMapRef:
                name: platform-runtime
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: platform-secrets
                  key: database-url
                  optional: true
