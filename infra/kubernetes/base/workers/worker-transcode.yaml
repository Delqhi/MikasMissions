apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker-transcode
  namespace: mikasmissions-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: worker-transcode
  template:
    metadata:
      labels:
        app: worker-transcode
    spec:
      containers:
        - name: worker-transcode
          image: ghcr.io/delqhi/mikasmissions/worker-transcode:latest
          envFrom:
            - configMapRef:
                name: platform-runtime
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: platform-secrets
                  key: database-url
                  optional: true
